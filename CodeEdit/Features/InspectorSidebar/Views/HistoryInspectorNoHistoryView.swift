import SwiftUI

struct HistoryInspectorNoHistoryView: View {
    var body: some View {
        VStack {
            Text("No History")
                .font(.system(size: 16))
                .foregroundColor(.secondary)
        }
    }
}
