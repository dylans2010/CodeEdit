import SwiftUI
import Preferences

struct PreferencesPlaceholderView: View {
    var body: some View {
        VStack {
            Text("Implementation needed")
        }
        .frame(width: 844, height: 300)
        .padding(30)
    }
}
